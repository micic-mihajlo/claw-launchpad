{
  "name": "@clawpad/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Claw Launchpad API (connectors + provisioning control plane).",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@hono/node-server": "^1.17.1",
    "better-sqlite3": "^12.6.2",
    "hono": "^4.9.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.2.3",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3"
  }
}
